# Demonstration Runbook — EvidenceGraph 5-Minute Submission Script

## 1. Overview

This script outlines a crisp, compelling 5-minute walkthrough of EvidenceGraph, proving the entire pipeline operates on real Razorpay events with zero fake metrics, zero fabricated evidence, and mathematical auditability.

---

## 2. 5-Minute Timeline

### 0:00–0:30 — Problem Statement & The Blind Spot
- **Talking Points**: "Current payment risk systems provide risk scores, but they cannot answer: *How trustworthy is the underlying evidence? Did the captured event come from an independent source, or a duplicate webhook? Are observations contradicted across lifecycle events?*"
- **Visual**: Open frontend at `http://localhost:5173`. Show the unified platform header.

### 0:30–1:00 — System Operations & System Truth (Phase 19)
- **Talking Points**: "EvidenceGraph maintains two planes of truth: *Domain Truth* and *System Truth*. Before looking at a payment, we inspect the operational integrity of the system itself."
- **Action**: In the **System Operations & Pipeline** view:
  - Highlight the 100% real health status (Database, Redis, Worker, Ingestion).
  - Highlight true queue depth, processing lag, and conservative pipeline watermark.
  - Click **"Run System Verification"** to demonstrate live evaluation of the 10 System Invariants (`INVARIANT_SYS_01` to `INVARIANT_SYS_10`).

### 1:00–1:45 — Golden Payment & Real Razorpay Webhook Ingestion
- **Talking Points**: "Switch to Payment Evidence & Intelligence. We select a real payment processed via Razorpay Test Mode."
- **Action**: Select the Golden Payment (`pay_QviWlM1wGsqQzD` / target payment).
  - Show the canonical payment lifecycle (`first_observed_at`, `last_observed_at`, `amount`, `status`).
  - Show the **Processing & Freshness Status** card demonstrating analytical layers are `CURRENT`.

### 1:45–2:30 — Reconciled Facts, Coverage & Multi-Dimensional Reliability
- **Talking Points**: "Atomic observations from disparate webhooks are normalized into canonical Evidence Facts. We measure evidence coverage against explicit requirement profiles and calibrate uncertainty across 7 distinct dimensions."
- **Action**:
  - Show **Reconciled Evidence Facts** (e.g. `payment_captured`, `order_paid`).
  - Show **Evidence Coverage** (`COMPLETE`, 0 missing critical fields).
  - Show **Evidence Reliability** ($R = 0.94$) and its calibrated uncertainty boundary $[0.88, 1.00]$.

### 2:30–3:15 — Integrity Decision Trace & Cryptographic Auditability (Phases 9, 10)
- **Talking Points**: "We do not guess confidence. The Composite Integrity score is calculated across quality, consistency, and corroboration. Every decision creates an immutable SHA-256 decision trace chained to previous traces."
- **Action**:
  - Scroll to **Evidence Integrity**. Show dimensional breakdown and active integrity score.
  - Highlight the cryptographic trace ID and SHA-256 hash.

### 3:15–4:00 — End-to-End Lineage & Causal Explanation (Phase 14)
- **Talking Points**: "Operators need full causal lineage: *Why was this decision reached? Which webhook originated which observation?*"
- **Action**:
  - Expand **End-to-End Lineage DAG**.
  - Show the clean graph: `WebhookEvent` $\to$ `PaymentEvent` $\to$ `EvidenceObservation` $\to$ `EvidenceFact` $\to$ `EvidenceIntegritySnapshot`.
  - Read the deterministic causal explanation generated by the Lineage Engine.

### 4:00–5:00 — Historical Replay & Differential Analysis (Phase 18)
- **Talking Points**: "When audited 6 months later, what if new evidence arrived? EvidenceGraph guarantees deterministic point-in-time decision replay and differential analysis."
- **Action**:
  - Scroll to **Decision Replay & Differential Analysis**.
  - Select timestamp $T_1$ (pre-capture) and click **"Replay Decision State"** to show historical state with zero future leakage.
  - Switch to **"Differential Analysis (T1 vs T2)"** and click **"Run Differential Analysis"** to display the categorized changes (`FACT_ADDED`, `RELIABILITY_SHIFT`) and plain-English explanation.
- **Closing**: "EvidenceGraph delivers complete mathematical transparency, causal lineage, deterministic replay, and live operational intelligence for payment evidence."
